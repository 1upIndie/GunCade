/// @description Hier Beschreibung einfügen
// Sie können Ihren Code in diesem Editor schreiben

     


#region mouse parallax

// parallax area
/*


  if(mouse_x >= camX and mouse_x <= camX + camWidth + parallax_X ){
   if(mouse_y >= camY and mouse_y <= camY + camHeight - parallax_Y ){
   parallaxOn = true;
  var widH = (camWidth/2)
  var wid = camX  + (camWidth/2); 
  
  var percParallax = 0;
  var parallaxPosNeg = 1;
   var resultParallax = point_distance(wid, mouse_y, mouse_x, mouse_y ); 
 
 if(  mouse_x <  wid  ){ percParallax = ( resultParallax/ widH); parallaxPosNeg = -1  }
 if(  mouse_x >  wid  ){ percParallax = ( resultParallax/ widH); parallaxPosNeg =  1  }    
 
 // move background for parallax effect horiontally
 parallaxLerped = lerp( percParallax, parallaxLerped, parallaxXLerp)
 // x =  wid + ( parallaxXMax * parallaxPosNeg * parallaxLerped ); 
 
   }
}
*/



#endregion	
